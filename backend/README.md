# NEXUS Backend

Python + FastAPI backend for NEXUS.

The backend is organized around API, application, domain, and infrastructure boundaries. Backend tests live under `backend/tests/` and remain separate from frontend tests.
